import { OnboardingFlow } from "@/features/onboarding/OnboardingFlow";

// /onboarding — the post-sign-in flow: tell your story, make your avatar, design
// your card backings, then watch your social graph populate.
export default function OnboardingPage() {
  return <OnboardingFlow />;
}
